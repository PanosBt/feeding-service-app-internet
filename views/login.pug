extends layout

block content
    //h1= title
    //p Welcome to #{title}
    //p This is a homepage
    br
    div(class="login-box")
        div(class="center-stuff")
            form(method='POST' action='/login')
                div
                    //label(for='username' class="my-display") Username
                    input(type='text' id='username' placeholder='username' name='username' class="my-display center-stuff text-input")
                br
                div
                    //label(for='password' class="my-display") Password
                    input(type='password' id='password' placeholder='password' name='password' class="my-display center-stuff text-input")
                br
                button(class = 'my-button') Log in
            if authorize === false
                p(class="error-color") Λάθος στοιχεία!
            if data_nit === false
                p(class="error-color") Λυπούμαστε. Η καταχώρηση των στοιχείων σας στο σύστημα δεν έχει ολοκληρωθεί ακόμα.
